# A-Local-Defence-Against-Poisoning-Attack-on-Federated-Learning
This project implements LoMar (Local Model Reputation), a defense mechanism against data poisoning attacks in Federated Learning (FL) environments. The system simulates a federated learning setup where client devices train CNN models locally on the MNIST dataset and upload model parameters to a central server.
